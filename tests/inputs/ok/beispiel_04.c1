// Testprogramm aus Testsuite

float test(int x, int y) {
    int a = 1;
    int b = 2;
    float c = 3.5;
    float d = 4.5;
    bool e = true;
    bool f = false;

    c = a + b * c / (d * a) - (a / d);
    c = a = b;
    c = d = a;

    return a + b;
}

void main() {
    int a = 1;
    int b = 2;
    print(test(a, b));
}
